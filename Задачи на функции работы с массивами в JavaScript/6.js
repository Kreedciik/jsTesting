// Дан массив ['js', 'css', 'jq']. Выведите на экран последний элемент.

'use strict'

let arr = ['js', 'css', 'jq'],
    deletedElement;

deletedElement = arr.pop();

console.log(deletedElement);